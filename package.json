{
  "name": "crm-bonus",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run generate --workspace=@repo/database"
  },
  "devDependencies": {
    "prisma": "5.22.0"
  }
}
